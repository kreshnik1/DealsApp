import SwiftUI

struct StoresView: View {
    var body: some View {
        PlaceholderFeatureView(
            title: "Stores",
            systemImage: "building.2.fill",
            message: "Nearby discovery and store browsing will live here next."
        )
    }
}
